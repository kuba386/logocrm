node_modules/
.pnpm-store/
dist/
.next/
out/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local
.env*.local
# Sentry wizard кладёт сюда SENTRY_AUTH_TOKEN; под .env*.local не попадает
.env.sentry-build-plugin
next-env.d.ts
packages/db/supabase/.branches/
packages/db/supabase/.temp/

# Персональные настройки Claude Code (.claude/settings.json — командный)
.claude/settings.local.json

# Посторонние каталоги в рабочей копии.
#
# logoped-crm — старый одно-тенантный MVP, 589 МБ, лежит внутри рабочего
# каталога LogoCRM по историческим причинам. Это отдельный проект с другой
# схемой и своей базой; в этот репозиторий он не входит.
/logoped-crm/

# Дубль: рабочий middleware живёт в apps/web/middleware.ts.
/middleware.ts

# Материалы из Stitch. Убрать отсюда, когда дойдём до редизайна и решим,
# что из этого хранится в репозитории.
/design/

# Playwright
apps/web/e2e/.auth/
apps/web/playwright-report/
apps/web/test-results/
