# Этап 8 — SaaS: тарифы, лимиты, онбординг, prod

Дата: 2026-09-22 — 2026-09-24 (8a закрыта 24.09.2026; 8b не начата)
PR: #108 (план), #110 (0049), #111 (план read-only), #112 (0050), #113
    (0051), #114 (/admin), #115 (0052), #116 (0053), #120 (0055), #121
    (0056), #123, #125, #126 (фиксы вокруг 0055/0056), #128 (0057).
    Вне 8a, между строк той же последовательности: #118/#119 (0054,
    финансовый багфикс, не эта тема), #117/#122 (деньги на карточке
    ребёнка), #129 (`/api/health`, подготовка к 8b)
Миграции: 0049–0053, 0055–0057 (0054 — не этот этап, см. ниже)

## Plan

Раздел написан до кода, по правилу «Перед этапом» (`docs/Roadmap/stages.md`).
Первая редакция прошла ревью `architect` (20 находок); ниже — уже с
правками. Пункты, появившиеся из ревью, помечены **(ревью)**.

### 0. Режем на две части, как этап 7

Промт этапа 8 — два разных продукта: **то, что делает LogoCRM
продаваемым** (тарифы, лимиты, trial, воронка, панель платформы) и **то,
что делает его безопасно выкатываемым** (второй Supabase-проект, GitHub
Environment `production`, Vercel prod, Sentry, prod-экземпляры n8n и
бота). Первое — код и миграции, проверяемые pgTAP и кликом на staging.
Второе — почти целиком ручные шаги владельца, и проверить его нельзя
нигде, кроме самого prod.

| Часть | Что внутри | Что даёт само по себе |
|---|---|---|
| **8a — продукт для продажи** | `plans`, лимиты триггерами, trial и read-only, заявки на оплату + `extend_subscription`, `/app/settings/plan`, баннеры, онбординг, воронка (`funnel_stage` + `funnel_events` + дашборд), `/admin` платформы, экспорт и заявка на удаление центра | Второй центр можно завести, ограничить тарифом и продлить по чеку — вручную, но без правок в базе |
| **8b — prod-контур** | `logocrm-prod`, `production` environment, `deploy-prod.yml` по кнопке, Vercel prod и домен, Sentry, n8n/бот prod, ADR-010 production checklist, публичная витрина `/book/[slug]` (последней: она первая смотрит наружу) | Реальный центр работает не на staging |

Порядок: 8a целиком, потом 8b. 8a не требует ни одного нового сервиса и
ни одного секрета; 8b упирается в владельца на каждом шаге. Витрина
`/book/[slug]` ушла в 8b сознательно: первый экран без сессии, его rate
limit и защита от спама — вопросы того же ряда, что prod-firewall.
**(ревью)** Следствие, записанное прямо: в 8a воронка измеряет только то,
что регистратор вводит руками; единственный автоматический производитель
лидов — `/book` — появится в 8b. На приёмке 8a пустой дашборд конверсии
— не баг.

Мультифилиал (`branches`, п. 5 промта) — **не в этом этапе**: ни у одного
из известных центров второго адреса нет, таблица без потребителя — тот же
класс, что `exercise_library` до `/app/library` (0038 Р10). В Backlog как
кандидат на этап 9+.

### 1. Что меняю (8a)

#### Тарифы

- **`plans`** — справочник платформы: `code text primary key`, `name`,
  `price_tiyin integer` **(ревью: тыйыны, не сомы — иначе MRR на `/admin`
  смешает единицы)**, `limits jsonb not null` (`teachers`, `students`,
  `ai_notes_month`), `sort`. Ключей `telehealth`/`branches` **нет**: лимит,
  который никто не проверяет, — ложь того же класса, что денормализация
  с дырой на запись. Читать — любой авторизованный (экран «Сменить план»),
  писать — только `platform_admin`; политика `for select` руками, без
  `apply_tenant_rls`.
- **Seed тарифов — строками в самой миграции** (ревью), не в `seed.sql`:
  prod поднимается без seed, и пустая `plans` не должна означать
  «безлимит». `centers.plan` → **FK на `plans.code`** (`on delete
  restrict`), литеральный `check` из 0001 снимается — иначе два списка
  тарифов разойдутся при первом новом. Отсутствие строки плана или ключа
  лимита — **отказ записи, а не разрешение**; «без ограничения» кодируется
  явным значением (`-1`), не `null`. Цены и лимиты — «Требует
  пользователя».
- **Тариф пишет только платформа** (ревью). `grant update on centers to
  authenticated` + `centers_update_owner` (0001) дают владельцу центра
  запись во все колонки, включая `plan`, `trial_ends_at`,
  `subscription_until` и `settings->'features'` — то есть `PATCH` из
  devtools отменяет оплату навсегда (0032 Р9 отказался от отметки в
  `centers` ровно поэтому). Триггер `before update on centers`: изменение
  этих колонок и ключа `features` — только при `is_platform_admin()`,
  иначе `42501` с русским текстом. Колоночных прав по роли в Postgres нет
  (ADR-005) — поэтому триггер.
- **`billing_email` не на `centers`** (ревью): `centers_select_members`
  отдаёт строку целиком любому члену, включая родителя. Заводится
  `center_billing (center_id pk, email, contact_name)`, видна owner/admin,
  по ADR-005.
- **`platform_admins`** — `user_id primary key references auth.users`,
  `created_at`; предикат `is_platform_admin()` — `security definer` по
  этой таблице. У администратора платформы **нет `memberships`**, значит
  `current_center()`/`my_role()` — null: все `/admin`-RPC принимают
  `center_id` явным параметром, начинаются с `auth.uid() is not null` и
  `is_platform_admin()`, отказ **одинаковый (`42501`)** для «нет прав» и
  «нет центра» — иначе перебором uuid читается список всех центров
  платформы (оракул, от которого 0004 закрывал `invitation_preview`).
  `emit_event` — только с третьим аргументом. Действия платформы над
  центром пишутся в его `audit_log` (`centers` под `apply_audit`) —
  владелец видит, кто менял его тариф.

#### Платежи — без Storage в 8a (ревью)

- **`platform_payments`** — заявка центра и подтверждение платформы в
  одной строке, но с разными колонками: центр пишет `claimed_amount_tiyin`,
  `claimed_months`, `source`, `note`; платформа пишет `confirmed_by`,
  `confirmed_at`, `months`, `plan`, `receipt_received`. Чек **не
  хранится**: фото уходит владельцу платформы в Telegram отдельным
  сообщением с `payment_id` в тексте, в базе — только факт и отметка
  «чек получен». Storage со своим ADR и приёмкой — когда появится второй
  потребитель. Политики: `for select` owner/admin своего центра и
  `platform_admin`; запись — только RPC; `apply_tenant_rls` к таблице
  **не применяется** (она дала бы `for all` с `with check`, и владелец
  центра дописал бы себе подтверждение прямым `insert`).
- `submit_platform_payment(claimed_amount_tiyin, source, claimed_months,
  note)` — owner; событие `platform.payment_submitted` → `platform_admins`
  с привязкой Telegram.
- `extend_subscription(payment_id, plan, months)` — **срок, сумма и план
  — параметры действия администратора платформы, не поля заявки**
  (ревью): заявка на 12 месяцев за месячную цену не должна продлевать
  год. Идемпотентность — `update … where id = ? and confirmed_at is null
  returning`, ветвь «уже подтверждено» с отказом, частичный unique-индекс;
  `subscription_until = greatest(now(), subscription_until) + months`,
  событие `subscription.extended` — только если строка реально изменилась.

#### Лимиты — инварианты, не проверки

- **Лицензия специалиста = живая карточка `teachers` (`deleted_at is
  null`)** — одно определение, записанное в `docs/Database.md` (ревью).
  Триггер лимита — только на `teachers`; триггер на `memberships` не
  считает, а отбивает membership без карточки. Иначе `create_invitation`
  (карточка до регистрации) и `accept_invitation` (членство после)
  расходуют одну лицензию дважды в одном счётчике и ни разу в другом.
- **Лимит учеников считает живые строки (`deleted_at is null`)
  независимо от статуса** (ревью): иначе 300 детей в `completed` держат
  карточки на тарифе за 20.
- **Только на переходе в считаемое состояние** (ревью): `insert` и
  `update`, увеличивающий счёт (восстановление из архива). Центр, уже
  превышающий лимит после понижения плана, живёт дальше и теряет только
  право расти — иначе правка телефона у ребёнка отбивалась бы лимитом,
  включая архивацию, которой он бы в лимит и вошёл.
- **Гонка** — `pg_advisory_xact_lock(hashtext('center_limit:' ||
  center_id))` **до** `count(*)`, одним и тем же ключом во всех триггерах
  лимитов (иначе гонка переезжает между таблицами). `serializable` не
  годится: под PostgREST транзакцией управляет не приложение, а `40001`
  требует повтора, которого нет.
- Русский текст отказа: «Тариф Solo: 1 специалист. Перейдите на Studio»,
  `errcode 23514`; имя констрейнта/триггера — в `errors.ts`.

#### Trial и read-only — гарантия в двух местах (ревью)

Политики RLS **не применяются к `security definer`-функциям** — а почти
вся запись в проекте идёт через них (`mark_attendance`, `complete_lesson`,
`sell_subscription`, `record_payment`, `create_lesson_series`,
`write_lesson_note`, `create_student_with_payer`, …). Режим «только
чтение» через `with check` политик закрыл бы прямой `PATCH` и оставил
открытым главный путь.

- **Основной гейт** — `assert_center_writable(center_id)` первой строкой
  каждой definer-функции записи, рядом с `auth.uid() is not null`:
  `plan = 'trial' and trial_ends_at > now()` или `subscription_until >
  now()`; отказ — свой `errcode` и русский текст «Подписка центра истекла
  — обратитесь к администратору». Текст даёт функция, политика —
  гарантия без текста (тот же приём, что `payments_refund_once_key`).
- **Второй рубеж** — `center_writable()` в `with check` через переиздание
  `apply_tenant_rls`/`apply_role_rls` (закрывает прямой PostgREST).
- **Полноту держит забор pgTAP по каталогу** (как 0028/0031 держат
  политики): список definer-функций, пишущих в таблицы центра, минус
  белый список (`submit_platform_payment`, `switch_center`, `emit_event`,
  очередь `notification_*`, `ai_job_*`) должен быть пуст.
- **Контур бота при просрочке** — решение, а не тишина (урок 0047):
  `request_voice_note` отказывает с русским текстом (диктовка не
  начинается — деньги не тратятся); `report_voice_note` для уже
  выданного токена — отвечает специалисту «подписка истекла»; уведомления
  родителям (напоминания, ДЗ) **продолжают идти** — они не стоят
  платформе и нужны детям, которые продолжают ходить.
- owner при просрочке сохраняет чтение и `submit_platform_payment`;
  остальные роли — только чтение.
- Событие `trial.ending` за 3 дня — из `schedule` (раз в час), доставка
  owner через `notification_admin_targets`; **дни до конца считает SQL в
  поясе центра** и отдаёт готовым числом вместе с `center_writable()`
  (ревью: `new Date()` в браузере уехавшего администратора даёт другой
  день).

#### Квота ИИ (ревью)

- `check_ai_quota(center_id)` по `ai_usage` за календарный месяц центра.
- **Отказ — на `request_voice_note`**, до того как человек нажал
  «записать»: там есть сессия, роль и место для русского текста. В
  `ai_job_begin` квота остаётся гарантией (Б3 из 0042: запись всё равно
  упадёт), но **не** третьей формой ответа «объект с `refused`» — воркер
  знает только «null / объект» (0048). Отказ в `ai_job_begin` = `null` +
  событие `ai.quota_exceeded` с доставкой специалисту через
  `event_messages`; в `n8n/README.md` — ветка именованным инвариантом.
  Ассерт 0048 на шесть ключей успешного ответа не трогается.

#### Воронка — этап отдельно от состояния (ревью)

- **Колонка `students.status` не расширяется.** Семь шагов и три
  состояния в одной колонке — урок 0015 (`status='frozen'` затирал
  реальность): лид на диагностике, ушедший на паузу, терял бы шаг
  навсегда. Заводится **`students.funnel_stage`** (`lead`, `contacted`,
  `consultation`, `assessment`, `trial`, `active`, `completed`) с FK на
  lookup-таблицу **`funnel_stages`** (`center_id is null` — дефолт
  платформы; «статусы — lookup-таблицы, не литералы»). `status`
  (`active/paused/archived`) остаётся состоянием рядом с этапом.
- **`funnel_events`** — `student_id`, `center_id`, `from_stage`,
  `to_stage`, `at`, `by`, `cause` **(ревью: не `source` — у `students`
  уже есть `source` «откуда пришёл лид»)**, `is_service boolean`. Пишется
  триггером на `students.funnel_stage` (и на insert, `from_stage null`).
  Денормализованная история: `grant select`, политики `for select`
  руками (owner/admin/registrar), **без `apply_tenant_rls`**; teacher,
  parent, finance — ни строки (коммерческие данные).
- **Переходы — явный список по ролям в триггере**: политика на `update`
  для registrar/admin и есть прямой путь, поэтому «переходы в функции»
  не защищают. `restore_student` возвращает **предыдущий этап из
  `funnel_events`**, не `active` (ревью: иначе восстановление лида
  фабрикует конверсию). Служебные переходы (архив, восстановление, ручная
  правка) помечаются `is_service` и `funnel_summary` их исключает.
- Автопереход `→ active` при продаже абонемента и первом посещении — в
  триггерах на `subscriptions`/`attendance`, только из этапов до `active`,
  `paused`/`archived` не трогая.
- `funnel_summary(from, to)` — по `funnel_events`, не по текущему этапу.
  Источники лидов — из `students.source`.

#### Функции

`is_platform_admin()`, `assert_center_writable(uuid)`,
`center_writable()`, `center_limits()` **(ревью: текущие значения, лимиты
и остаток одним запросом — тем же, что и триггер; экран только рисует,
права и занятость в браузере не считаются)**, `check_ai_quota(uuid)`,
`submit_platform_payment(...)`, `extend_subscription(uuid, text,
integer)`, `funnel_summary(date, date)`, `export_center()`,
`export_center_audit(date, date)`, `request_center_deletion()`.

- **`export_center()`** — через RLS каждой таблицы (экспорт — вынос базы
  клиентов). Список таблиц **строится из каталога** (все с колонкой
  `center_id`) минус явный deny-list (`telegram_accounts` — личные чаты;
  `lesson_voice_requests` — токены); **забор pgTAP**: таблица с
  `center_id`, которой нет ни в экспорте, ни в deny-листе, роняет CI
  (ревью: иначе `branches` этапа 9 молча выпадет). `audit_log` —
  отдельным вызовом с диапазоном дат: у платящего центра это самая
  большая таблица.
- **`request_center_deletion()`** — `deleted_at` + событие; физическая
  очистка через 30 дней — **не в этом этапе**: первый `delete` в проекте
  требует своего ADR (ADR-012).
- **Самоподписка** (ревью): `create_center` не ограничен — один аккаунт
  заводит сколько угодно центров с 14-дневным trial каждый; с лендингом
  это и бесплатный корм, и обход лимита учеников. Ограничение: **не
  больше одного центра на trial у одного пользователя-владельца**
  (триггер на `centers`/`memberships`), второй — только через платформу.

#### Экраны

- `/app/settings/plan` — из `center_limits()`: план, лимиты с остатком,
  дни до конца, история заявок, «Сменить план», «Оплатить» (реквизиты
  Mbank/Elcart из настроек платформы, инструкция «фото чека — в Telegram
  владельцу платформы, номер заявки в тексте»).
- Баннер trial/expired в `app/layout.tsx` — из того же RPC, три
  состояния.
- Онбординг-чеклист owner/admin — из `center_limits()` (те же счётчики),
  без таблицы «прогресса».
- `/app/funnel` (owner/admin/registrar) — `funnel_summary`, застрявшие с
  кнопкой WhatsApp.
- `/admin` (`platform_admin`, отдельный layout без выбора центра) —
  центры, план, срок, заявки, «Подтвердить платёж» (план, месяцы), MRR по
  `platform_payments` в тыйынах.
- Лендинг — перенос в `apps/landing`, отдельный PR без базы.

### 2. Риски

1. **Забор definer-функций записи.** Самая широкая правка доступа с
   0028: каждая пишущая RPC получает `assert_center_writable`. Пропуск
   одной — просроченный центр пишет через неё; лишняя — живой центр
   перестаёт. Забор по каталогу обязателен, белый список — явный.
2. **Триггер на `centers`** перекрывает лишнее (`name`, `settings.city`
   владельцу) — тест «owner меняет название, не меняет тариф».
3. **Лимиты и гонка** — advisory lock одним ключом; тест двух сессий по
   образцу unique-гонки 0012/0023.
4. **`funnel_stage` + триггер переходов** задевают `students` (2 этап),
   `restore_student` (0005), `sell_subscription`/`mark_attendance`
   (0008/0009): существующие pgTAP обязаны остаться зелёными без правки
   ожиданий.
5. **`ai_job_begin` переиздаётся четвёртый раз за неделю** (0042, 0048,
   теперь квота) — состав ключей держит тест 0048.
6. **`platform_admin` — первая роль вне центра**: `revoke` на каждой
   новой функции, `0007_function_grants` пополняется всеми.

### 3. Что может задеть

- `apply_tenant_rls`/`apply_role_rls` — все таблицы центра; заборы 0028 и
  0031 — новые таблицы туда не попадают, ожидаемые наборы для
  `funnel_events`/`platform_payments` добавляются явно.
- `centers` — FK на `plans`, триггер защиты тарифа, `check` снят.
- `students` — `funnel_stage`, триггеры; `restore_student`.
- `subscriptions`/`attendance` — триггеры автоперехода.
- `ai_job_begin`, `request_voice_note`, `report_voice_note` — квота и
  просрочка.
- `event_messages` — пять типов (`trial.ending`,
  `subscription.extended`, `platform.payment_submitted`,
  `ai.quota_exceeded`, `center.deletion_requested`) с шаблонами по
  умолчанию, переменными по каналу и **швом** по накопленным событиям в
  той же миграции (правило в `docs/Database.md`).
- `errors.ts` — новые констрейнты в `CHECK_MESSAGES`, ветка кода
  просрочки (не подмена `42501` вообще).
- `apps/web`: `layout.tsx` (баннер, онбординг), `students/*` (этап
  воронки — бейдж, фильтры, `STUDENT_STATUS_*` в `lib/students.ts`,
  `helpers.ts`, `dto.ts` — одним проходом с Vitest/pgTAP на одинаковых
  входах), три новых экрана, `apps/landing`.
- Документы: `docs/Database.md` (тарифы, «живой специалист», закрытые на
  запись таблицы 8a), `docs/FEATURE_MATRIX.md` (актор `platform_admin` —
  сейчас его нет), `docs/BUSINESS_RULES.md`, `CHANGELOG`, `INDEX.md`,
  `КАРТА_ПРОЕКТА.md`; ADR-010 production checklist, ADR-011 read-only и
  тариф вне рук центра, ADR-012 физическое удаление.
- **teacher и parent:** при просрочке — запись закрыта с русским текстом
  из гейта; уведомления родителям продолжают идти; специалист не видит
  ни планов, ни платежей, ни воронки.

### 4. Критерии готовности

Чек-лист этапа (`stages.md`) п. 1–3 и 5 (4 и 6 — в 8b), плюс pgTAP в
стиле 0028/0031 (заборы по `pg_policies`/каталогу в начале файла,
`tests_claims()` перед каждым блоком, видимость через `is(count, N)`):

- **просрочка на пути RPC, не только политик**: центр с `trial_ends_at` в
  прошлом — `mark_attendance`, `complete_lesson`, `sell_subscription`,
  `record_payment`, `create_lesson_series`, `write_lesson_note`,
  `create_student_with_payer` отдают отказ со своим кодом и русским
  текстом; `submit_platform_payment` и чтение проходят; забор definer
  минус белый список пуст;
- **тариф не пишется центром**: от owner `update centers set plan/…` —
  отказ, `set name` — проходит; от `platform_admin` через
  `extend_subscription` — проходит;
- **лимиты**: второй специалист на solo — `23514` с текстом; после
  продления на studio — проходит; гонка двух вставок даёт одну;
  понижение плана — правка и архивация проходят, рост отбивается;
  `create_invitation` + `accept_invitation` — одна лицензия; после
  `revoke_membership` замена проходит;
- **продление идемпотентно**: по одному `payment_id` дважды — второй
  отказ, `subscription_until` не изменился, второго события нет;
- **воронка**: переход пишет обе стадии; `restore_student` не создаёт
  конверсии; `funnel_summary` считает по истории; teacher/parent/finance
  видят 0 строк (`is(count, 0)`); запрещённый переход отбивается;
  автопереход не трогает `paused`/`archived`; тесты 0008/0009/0023
  зелёные без правок;
- **квота**: `request_voice_note` при исчерпании — отказ с русским
  текстом; `ai_job_begin` — `null` + событие; успешный ответ — ровно
  шесть ключей;
- **платформа**: `is_platform_admin()` от обычного пользователя — false;
  `/admin`-RPC от чужого — `42501` одинаковый для чужого и
  несуществующего центра; `platform_admin` без `memberships` не падает;
- **экспорт**: забор «таблица с `center_id` — либо в экспорте, либо в
  deny-листе»; от admin чужого центра — ни строки; `telegram_accounts`
  не выгружается;
- **изоляция** новых таблиц: чужой центр не видит строку;
- **самоподписка**: второй trial-центр тому же владельцу — отказ;
- `0007_function_grants` пополнен; `pnpm db:types`; `get_advisors` после
  миграции; Playwright: баннер просрочки, отказ по лимиту, `/admin` —
  фикстура второго центра с истёкшим trial.

### Требует пользователя

- Цены тарифов (в сомах, в базе — тыйыны) и лимиты по каждому — до
  первой миграции 8a.
- Реквизиты Mbank/Elcart для экрана оплаты; Telegram владельца платформы
  (кто получает чеки); кто `platform_admin`.
- Подтвердить: мультифилиал — в этап 9+; чек не хранится в приложении;
  один trial-центр на владельца.
- Для 8b — всё из «Требует пользователя» промта этапа: prod-проект
  Supabase, секреты `production`, домен, Vercel, Sentry, ручной
  deploy-prod.

### Решение по механизму read-only (после второго ревью архитектора, 22.09.2026)

Гейт в каждой из ~70 definer-функций записи **заменён триггером на
таблицах**: один `before insert/update/delete` guard на каждой таблице
центра, навешивается циклом по каталогу. Триггер срабатывает и внутри
`security definer`, и на прямом PATCH; политики RLS вторым рубежом не
переиздаются. Условия, без которых механизм не работает:

1. **Guard срабатывает только при `auth.uid() is not null`.** Единственный
   признак сессии, который переживает и definer, и `set role`.
   Следствие, записанное как граница (ADR-011): миграции, RI-каскады
   (`on delete set null` при удалении пользователя) и контур `bot_worker`
   триггером не покрыты. Деньги контура бота режутся узкой проверкой
   внутри четырёх функций: `request_voice_note`, `report_voice_note`,
   `ai_job_begin`, `ai_write_lesson_note`; ответ специалисту — русским
   текстом через `event_messages`.
2. **Путь разблокировки не может быть под блокировкой.** Исключения:
   `audit_log`, `events` (иначе `extend_subscription` откатится на своём
   же аудите), `platform_payments`, `center_billing`. Первый pgTAP —
   «платформа продлевает просроченный центр, и он снова пишет».
3. **Режим отказывает на входе в работу, никогда на её закрытии.**
   Исключены `notification_log`, `lesson_reminders_sent`,
   `center_digest_runs`, `ai_jobs`, `ai_usage`, `lesson_confirmations` —
   иначе вечный ретрай воркера.
4. **Белый список — по паре (таблица, операция).** `memberships` и
   `invitations`: delete/update свободны (уход сотрудника не зависит от
   оплаты), insert блокируется. `is_platform_admin()` проходит всегда.
5. **Fail closed.** `center_writable`: явный `case` по `plan`,
   `coalesce(…, false)`; `null` в датах — отказ. Инвариант на `centers`:
   trial требует `trial_ends_at`. Шов той же миграцией: существующим
   центрам проставить живой срок (тестовый центр владельца на staging уже
   просрочен 21.09), фикстуры 0017/0049 и seed — с живым сроком.
   Шов затирает исходные сроки, поэтому перед деплоем 0050 список
   затронутых центров снимается запросом и прикладывается сюда:
   `select id, name, plan, trial_ends_at, subscription_until, deleted_at
   from centers where (plan = 'trial' and (trial_ends_at is null or
   trial_ends_at < now())) or (plan <> 'trial' and (subscription_until
   is null or subscription_until < now()))`. Staging 22.09.2026: одна
   строка — «Логопед Плюс», trial до 21.09.2026 11:45 UTC, не удалён.
6. **Nullable `center_id`** (`message_templates`, `exercise_library`,
   `audit_log`) — в guard явная ветка: `center_id is null` из сессии
   центра — отказ, не пропуск.
7. **Забор двусторонний:** каждая таблица `public` либо под guard на
   i/u/d, либо в явном списке исключений с причиной; список nullable
   `center_id` зафиксирован `is(...)`. `telegram_*` — вне режима, в
   списке с причиной.
8. **Имя триггера сортируется первым** среди BEFORE-триггеров таблицы:
   просроченный центр с превышенным лимитом должен видеть «подписка
   истекла», а не «лимит тарифа».
9. **SQLSTATE `PT402`** (соглашение PostgREST → HTTP 402), не `42501`
   (занят под права) и не произвольный класс (PostgREST отдаст 500).
   Проверить на версии PostgREST проекта до коммита; в `errors.ts` —
   отдельная константа.
10. **Без кэша через `set_config`** — риск межцентровой утечки на
    соединении пула; поиск по PK `centers` на строку не измеряется.
11. **Момент отсечения** — грейс до конца дня в поясе центра, одно
    выражение в `center_writable`, `center_limits().days_left` считает
    тем же; общий набор случаев в Vitest и pgTAP.
12. **Что центр обязан мочь в read-only** (ADR-011): подать заявку на
    оплату, сменить центр, править название и пояс, экспорт, заявка на
    удаление, отвязать Telegram, отозвать доступ сотруднику; уведомления
    родителям идут.
13. **DELETE** под guard — стоимость нулевая, физическая очистка ADR-012
    не станет дырой.


## Шаг 1: миграция 0049 — тарифы, роль платформы, лимиты

PR #110. Справочник тарифов, роль `platform_admin`, лимиты специалистов и
учеников триггерами.

### Что это даёт

У центра появляется тариф с ценой и лимитами (Solo/Studio/Center), и
владелец платформы (по подтверждённому email, до какой-либо роли в
конкретном центре) может им управлять. Второй специалист сверх лимита
тарифа получает понятный отказ по-русски, а не тихо создаётся. Экран
тарифа получит одну функцию (`center_limits()`), которая отдаёт текущие
значения, лимиты и остаток — права и счёт считает база, не браузер.

### Отступления от ТЗ

- **Лимит учеников считает не-архивных, а не «живые строки»**, как
  планировал Plan Р5 (`deleted_at is null` независимо от статуса): у
  `students` нет мягкого удаления в буквальном смысле, `archive_student`
  меняет только `status`, а не `deleted_at`. Решение и причина — в
  `docs/BUSINESS_RULES.md`.
- Текст отказа лимита — без склонения числа специалистов («1 специалист»
  для любого N), чтобы не заводить русскую плюрализацию ради одной фразы.
- `plans`/`platform_admins` — без `apply_audit`: это справочники
  платформы, не данные центра, обычный аудит-триггер писал бы в
  несуществующий `center_id`.
- Ключ `ai_notes_month` в `limits jsonb` объявлен, но гейт на него —
  следующей миграцией (0053): в 0049 квоты ИИ ещё нет, значение просто
  не с чем сверять.

### Что нашло ревью

Ревью плана — 20 находок, ревью написанного SQL — 12, все закрыты до
мержа и записаны в шапке миграции как Р1–Р10.

### Тесты

`tests/0049_plans_and_limits.test.sql` — 41 pgTAP-проверка.


## Шаг 2: миграция 0050 — только чтение при истёкшей подписке

PR #112. Механизм из отдельного раздела Plan выше («Решение по механизму
read-only») реализован: один `BEFORE`-триггер на каждой таблице центра
вместо гейта в ~70 функциях записи.

### Что это даёт

Центр с истёкшей подпиской не может ничего записать — ни через
экран, ни прямым запросом в обход интерфейса — но продолжает читать
расписание и карточки, подавать заявку на оплату и экспортировать свои
данные. Родителям продолжают уходить напоминания и отчёты — они не
стоят платформе денег и нужны детям, которые продолжают ходить на
занятия независимо от того, оплатил ли центр тариф вовремя.

### Отступления от ТЗ

Само решение о механизме — не отступление от промта, а замена первой
редакции Plan (гейт в каждой функции) на вариант из второго ревью
архитектора: единый триггер на таблицах, guard срабатывает и внутри
`security definer`, и на прямом `PATCH`. Условия механизма (13 пунктов,
включая границу: guard видит только `auth.uid() is not null`, контур
`bot_worker` им не покрыт и режется отдельной узкой проверкой в четырёх
функциях диктовки) записаны в разделе Plan выше как решённый вопрос, а
не задним числом.

### Что нашло ревью

Первый круг ревью — 13 замечаний, закрыты. Повторный круг (ревью
написанного SQL) нашёл ещё, включая гонку в `center_writable` без
гранта — все закрыты до мержа.

### Шов

Существующим центрам с истёкшим/пустым сроком проставлен живой срок (30
дней от применения). На staging на 22.09.2026 это задело одну строку —
«Логопед Плюс», trial до 21.09.2026 11:45 UTC, центр не удалён.

### Тесты

`tests/0050_center_readonly.test.sql` — 43 pgTAP-проверки: забор по
каталогу (каждая таблица либо под guard, либо в списке исключений с
причиной), запись под просроченным центром отбивается и через RPC, и
через прямой PATCH, разблокирующие таблицы (`audit_log`, `events`,
`platform_payments`) сами не заблокированы.


## Шаг 3: миграция 0051 — заявки на оплату и продление подписки

PR #113 (миграция), PR #114 (экран `/admin`, стеком поверх той же базы).

### Что это даёт

Владелец центра подаёт заявку «я оплатил» с суммой и сроком — платформа
получает уведомление в Telegram и подтверждает или отклоняет с причиной.
Подтверждение продлевает подписку одним атомарным действием: план, сумма
и срок — параметры действия платформы, не поля заявки центра, поэтому
заявка на 12 месяцев по месячной цене не продлевает год автоматически.
Повторное подтверждение той же заявки — отказ, а не двойное продление.

### Отступления от ТЗ

- Уведомления центру об отклонении заявки в 8a нет — только сама заявка
  меняет статус, экран это покажет, push/сообщение — не в этом объёме.
- Коррекции ошибочного подтверждения (платформа продлила не тот план) в
  8a нет — по ADR-011 первая ошибка правится следующим явным действием,
  не откатом.
- Реквизиты Mbank/Elcart на экране оплаты — текстом, не QR-кодом, как
  предполагал промт.

### Что нашло ревью

Ревью плана — 14 находок, ревью написанного SQL — 10, все закрыты,
условия записаны в шапке миграции как Р1–Р14.

### Условие выката

Аккаунт владельца платформы должен быть зарегистрирован в LogoCRM с
подтверждённым email и привязанным Telegram — иначе уведомление
платформе выходит `skipped`, а `extend_subscription` отвечает 42501
всем, включая настоящего владельца платформы.

### Тесты

`tests/0051_platform_payments.test.sql` — 56 pgTAP-проверок.


## Шаг 4: миграция 0052 — напоминания о сроке, пульт платформы, один trial на владельца

PR #115.

### Что это даёт

Центр получает напоминание за 3, 1 и 0 дней до конца подписки — не
одно, а три ступени, и не может его выключить (это финансовый сигнал, а
не уведомление о занятии). Владелец платформы на `/admin` видит все
центры, их план и срок, выручку — без похода в базу напрямую. Один
человек не может завести себе бесконечное число бесплатных
14-дневных trial-центров: второй trial на того же владельца отклоняется.

### Отступления от ТЗ

- **Тип события из промта (`trial.ending`) заменён парой
  `subscription.ending`/`subscription.expired`** — единое событие не
  различало «скоро закончится» и «уже закончилось», а тексту и получателю
  это разное состояние.
- **MRR на `/admin` считается не суммой по `platform_payments`, а
  подписочной базой** (прайс тарифа × число живых платных центров): сумма
  фактических платежей — это исторические поступления, а не показатель
  текущей выручки в месяц, и эти две цифры разошлись бы при первой же
  ежегодной оплате.

### Что нашло ревью

Ревью плана — 13 находок, ревью написанного SQL — 8 (включая фолбэк
пояса в `center_timezone`, поведение «истёк» при открытой заявке на
оплату, ступени напоминания). Все закрыты, Р1–Р11 в шапке миграции.

### Известные границы

Owner/admin без привязанного Telegram получают `whatsapp_link` без
чата → доставка `skipped`. Планировщик напоминаний — четвёртый узел в
существующем сценарии `schedule` n8n, до его добавления напоминания не
уходят. Первый прогон после деплоя разошлёт «истёк» всем уже живым
просроченным центрам разом — ожидаемо, не повод для тревоги на приёмке.

### Тесты

`tests/0052_subscription_reminders.test.sql` — 46 pgTAP-проверок.


## Шаг 5: миграция 0053 — квота голосовых резюме по тарифу

PR #116.

### Что это даёт

Тариф ограничивает число голосовых резюме занятий в месяц. Специалист,
исчерпавший квоту, получает отказ с понятным текстом ещё на кнопке
«Записать голосовое» — до того, как диктовка потратила деньги на Whisper
и Claude. Счётчик учитывает не только уже оплаченные резюме, но и работы
«в полёте» (начатые, не завершённые) младше 8 минут — иначе параллельные
диктовки могли бы вместе превысить лимит незаметно для проверки.

### Отступления от ТЗ

Не отступление, а зафиксированный смежный дефект: перезахват зависшей
работы после сбоя n8n может дать вторую попытку распознавания, но
уникальный ключ `ai_usage_event_kind_key` (заведён ещё в 0042, не в этой
миграции) не пускает вторую строку `summary` в реестр расходов — счётчик
использования занижает на такую работу. Записано в шапке 0053 как долг
0042, не новый дефект этой миграции.

### Что нашло ревью

Ревью плана — 14 находок, все учтены, Р1–Р10 в шапке миграции.

### Тесты

`tests/0053_ai_quota.test.sql` — 37 pgTAP-проверок (после двух раундов
собственных фиксов теста, ещё до мержа — два реальных бага в самом
pgTAP-файле, не в миграции, и фикстура второго занятия).


## Не этап 8a: миграция 0054 — возврат за абонемент «на срок» (period)

PR #118 (миграция), PR #119 (фикс невалидного UUID-литерала в тесте,
через 4 минуты после мержа #118 — гонка публикации: правка теста
пушилась именно в момент, когда PR слился в старом виде, и `main`
оказался красным на джобе `db`).

Номер 0054 достался по порядку каталога, но по содержанию это не шаг
8a: финансовый багфикс из `docs/Backlog.md` (находка владельца
19.09.2026) — `refund_calc` для абонементов «на срок» всегда считал 0,
и кнопка «Вернуть» для такого абонемента никогда не появлялась.
Упомянут здесь только для полноты последовательности номеров; ни в
заголовке файла, ни в PR нет пометки «этап 8a». `tests/
0054_period_subscription_refund.test.sql` — 27 pgTAP-проверок.


## Шаг 6: миграция 0055 — воронка учеников

PR #120.

### Что это даёт

Каждый ребёнок проходит через явные шаги — от заявки до «Занимается» — и
это видно на отдельном экране: сколько лидов на каждом шаге, сколько
дошло до занятий за период, кто застрял без движения дольше двух недель
(с кнопкой написать в WhatsApp сразу оттуда). Переход не только руками:
продажа абонемента и первое посещение сами сдвигают ребёнка на «Занимается»,
если он ещё не там.

### Отступления от ТЗ

- **`archive_student`/`restore_student` не трогают `funnel_stage`
  вообще** — буквальное прочтение промта («restore возвращает предыдущий
  этап») прочитано как предостережение от автоматической фабрикации
  конверсии, а не как код: архивация/восстановление помечаются
  `is_service` и не считаются движением по воронке.
- `funnel_stages` — общий для платформы справочник, не per-center, хотя
  остальные подобные справочники в проекте центровые.
- Список источников лидов на дашборде строится по тем, кто вошёл в
  воронку за выбранный период, а не по всей истории центра.
- Кнопки ручного перехода этапа на карточке ученика видит только
  owner/admin; registrar управляет воронкой пока только с экрана
  `/app/funnel` целиком, не с карточки.

### Что нашло ревью

Ревью плана — 19 находок и 5 открытых вопросов по чтению спеки
(обозначены А–Д в шапке миграции), все закрыты до кода.

**Ревью написанного SQL этой миграции по факту так и не успело
запуститься до мержа** — PR #120 слился, пока CI не мог довести джоб `db`
до pgTAP: с 22.09 по 24.09.2026 `supabase start` падал на каждом
прогоне на инфраструктурной ошибке `ghcr.io toomanyrequests`, ни разу не
дойдя до тестов. Это открыло дорогу трём последующим находкам, часть из
которых — не про SQL, а про сам факт, что тест не запускался никогда:

1. **`fix(web): типы схемы вручную под 0055` (PR #123)** — `database.types.ts`
   не знал про `funnel_stage`/`funnel_summary`/`funnel_stuck`/
   `set_funnel_stage`, потому что бот, который обычно генерирует типы в
   CI, тоже не мог довести джоб `db` до конца. Типы дописаны вручную по
   DDL миграции.
2. **`fix(db): 0055 pgTAP — неоднозначный подзапрос плательщика`
   (PR #125)** — когда `ghcr.io` перестал блокировать прогон и pgTAP
   впервые реально выполнился, тест прервался на разделе «Граф», не
   дойдя до 49 из 63 проверок: `create_student_with_payer('Лид 0055', …)`
   в фикстуре создавал второго плательщика центра, и запрос вида
   `select id from payers where center_id = …` стал неоднозначным
   подзапросом. Правка — только тестовый файл, 15 мест.
3. **Ещё пять реальных багов найдено и починено в том же прогоне**, до
   отдельного PR — внутри ветки PR #121 (0056), поскольку расследование
   шло параллельно с той миграцией: опечатка в подмене UUID (13 hex
   вместо 12), temp-таблицы вне `set local role` недоступны под другой
   ролью, вставка `attendance` без сессии падает на `emit_event`
   (требует `auth.uid()`), пересечение времени двух вставленных занятий,
   `ok()` в pgTAP получал `integer` вместо `boolean`.

Итог: миграция, смёрженная с зелёным (по факту — никогда не запускавшимся)
CI, две с половиной суток простояла на `main` с непроверенным pgTAP.
Урок, который стоит держать в памяти: зелёная галочка CI ничего не
доказывает, если инфраструктурная ошибка не даёт джобу дойти до
содержательного шага — это не то же самое, что «тесты прошли».

### Тесты

`tests/0055_funnel.test.sql` — 63 pgTAP-проверки (после всех
описанных выше правок; в теле PR #120 на момент открытия было 54).


## Шаг 7: миграция 0056 — экспорт центра и заявка на удаление

PR #121 (несёт также фиксы теста 0055, см. выше).

### Что это даёт

Владелец может выгрузить все данные своего центра одним архивом и подать
заявку на удаление центра — с обязательным подтверждением по имени, без
случайного нажатия. Удаление — мягкое (`deleted_at` + событие), не
мгновенное стирание: у владельца есть время передумать и отменить заявку
до физической очистки.

### Отступления от ТЗ

**Экспорт реализован не так, как предписывал Plan.** Plan предполагал
«через RLS каждой таблицы» — обычный `select` от имени вызывающего,
где политики сами решают, что видно. По факту — `security definer` с
явным allow-list таблиц: список строится не как «каталог минус deny»
(что молча включило бы в выгрузку любую новую таблицу с `center_id`,
заведённую в будущей миграции), а как явный перечень с забором pgTAP,
падающим, если таблица не решена ни туда, ни в deny-лист.

Секреты — токены приглашений и пути к голосовым файлам — исключены из
экспорта целиком, не полями.

### Что нашло ревью

Два раунда ревью архитектора (план и написанный SQL) нашли реальные
дыры, все закрыты до PR: кросс-тенантная выгрузка через свободный
параметр `p_center_id` (убран — функции всегда берут `current_center()`);
утечка живых invite-токенов через экспорт и через `audit_log` (оба пути
закрыты deny-листом); гонка в идемпотентности заявки на удаление
(check-then-write, заменено на атомарный `update ... where deleted_at is
null`); спуфинг счётчиков аудита экспорта через аргумент из браузера
(счёт теперь считает сама функция, не принимает от вызывающего);
`submit_platform_payment`/`my_memberships()` не знали про удалённый
центр — переизданы.

### Post-merge

**PR #126** — три новых события (`center.exported`,
`center.deletion_requested`, `center.deletion_cancelled`) не получили
Zod-схему в `packages/contracts/src/events.ts` при написании 0056; гэп
найден на CI отдельного PR #125 (unit-тесты контракта проваливались,
поскольку та ветка тоже несла миграцию 0056), закрыт отдельным PR.

### Тесты

`tests/0056_center_export_deletion.test.sql` — 53 pgTAP-проверки.


## Шаг 8: миграция 0057 — публичная витрина записи /book/[slug]

PR #128.

**Расхождение с планом, зафиксированное явно, а не пропущенное.** Раздел
Plan этого отчёта (написан 22.09.2026, до кода) прямо относил `/book/[slug]`
к этапу **8b**, а не 8a: «Витрина `/book/[slug]` ушла в 8b сознательно:
первый экран без сессии, его rate limit и защита от спама — вопросы того
же ряда, что prod-firewall»; следствием того же решения Plan называл
пустой дашборд воронки на приёмке 8a («единственный автоматический
производитель лидов — `/book` — появится в 8b»). По факту 0057 смержена
как **восьмой шаг 8a**, и воронка на приёмке уже не будет пустой — этот
экран поставляет в неё лидов. Решение о переносе шага из 8b в 8a внутри
самой сессии, которая его выполняла, нигде отдельно не зафиксировано —
что стоит считать процессным упущением этого отчёта, а не тихой заменой
плана: если бы решение принималось осознанно, это тоже должно было бы
попасть в Plan-раздел выше, а не только в заголовок миграции.

### Что это даёт

Родитель заходит на `/book/[slug]` без регистрации, выбирает услугу,
специалиста и время — и оставляет заявку. Стойка видит её в
`/app/bookings`, подтверждает (тогда появляется ученик и занятие) или
отклоняет. Публикацию витрины центр включает и выключает сам —
`/app/settings/plan`.

### Отступления от ТЗ

**Заявка на подтверждение, а не «lead + planned lesson» напрямую**, как
буквально требовал и промт этапа, и чек-лист приёмки («/book/slug →
запись → lead + planned lesson + уведомление админу»). Решение владельца
после того, как первый раунд ревью нашёл: анонимная форма, пишущая
`lessons` напрямую, могла бы забить весь день специалиста фиктивными
занятиями через тот же `EXCLUDE`-констрейнт, что защищает настоящую
запись, — отказ в обслуживании. `booking_requests` — отдельная таблица,
анонимный путь не касается `students`/`lessons`/`payers` вовсе; ученик и
занятие появляются только когда стойка подтверждает заявку уже
авторизованной сессией.

Второе отступление того же происхождения, что и у 0055: `students.status`
на момент написания промта ещё принимал значение `'lead'` — к моменту
0057 это уже `funnel_stage` (0055).

### Что нашло ревью

Два раунда ревью плана (13, затем 22 находки — первый раунд не учитывал
ещё не принятое решение о заявке-на-подтверждение, второй пересчитал
план с нуля под него) и ревью написанного SQL (11 находок, включая
подтверждённый CI-блокер несовпадения имени типа в pgTAP-заборе и точное
повторение уже однажды исправленного бага 0037 Р2 — фильтр
`is_active` до `order by` в собственной копии `notification_admin_targets`,
дающий побег на дефолтный шаблон платформы вместо «не слать» при
выключенном центром канале). Почти все находки — следствие одного и
того же факта: публичная форма пишет в базу без пользовательской сессии,
и это меняет, какие проверки вообще применимы.

### Тесты

`tests/0057_public_booking.test.sql` — 47 pgTAP-проверок.


## Итог 8a

Всё из Plan для 8a, кроме одного добавленного шага, влито и прошло CI:
тарифы и лимиты (0049), только чтение при просрочке (0050), заявки на
оплату и продление (0051), напоминания и пульт платформы (0052), квота
ИИ (0053), воронка (0055), экспорт и удаление центра (0056) — и, сверх
исходного плана, публичная витрина записи (0057), которая должна была
достаться этапу 8b.

Владелец может: завести тариф и лимиты, увидеть баннер и отказ по
истечении подписки, подать заявку на оплату и получить продление после
подтверждения платформой, следить за воронкой лидов, выгрузить или
удалить данные центра, открыть публичную запись на сайте и разбирать
заявки из неё. Второй центр можно завести, ограничить тарифом и продлить
по чеку — без правок в базе, как и требовал Plan.

Что осталось за скобками 8a:
- **8b — prod-контур**: второй Supabase-проект, `production`
  environment, `deploy-prod.yml` по кнопке, Vercel prod и домен, Sentry,
  prod-экземпляры n8n и бота, ADR-010 production checklist. Все пункты —
  «Требует пользователя» с первой строки, начинать без владельца нельзя.
  PR #129 (`/api/health`, чек-лист observability) — уже подготовка к
  этой части, вне 8a.
- Чек-лист приёмки `docs/Roadmap/stages.md` пункт 6 (prod-сценарий целиком)
  — за пределами 8a по определению.
- Мультифилиал (`branches`) — сознательно не в этом этапе (Plan, раздел
  «0»), кандидат на этап 9+.

### Требует владельца (сквозной список по 8a)

- Цены тарифов и лимиты — **закрыто**: Solo 990, Studio 3 900, Center
  7 900 сом (0049).
- Реквизиты Mbank/Elcart, Telegram владельца платформы, кто
  `platform_admin` — **закрыто**, аккаунт зарегистрирован (условие
  выката 0051).
- JWT роли `public_booking` (`SUPABASE_BOOKING_JWT` в Vercel) — **открыто**,
  без него `/book/[slug]` не заработает; инструкция дана владельцу
  отдельно (тот же способ, что `SUPABASE_BOT_JWT` у бота).
- Для 8b — целиком: prod-проект Supabase, секреты `production`, домен,
  Vercel, Sentry, ручной `deploy-prod`.
