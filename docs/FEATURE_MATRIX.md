# Матрица прав

Кто что видит и может. Источник — код: RLS-политики миграций и гейты в
`security definer`-функциях, а не замысел.

Пополняется по этапам — см. Definition of Done в
[Roadmap/stages.md](Roadmap/stages.md). Почему правило именно такое — в
[BUSINESS_RULES.md](BUSINESS_RULES.md).

## Как читать

| Знак | Значение |
|---|---|
| **В** | видит |
| **С** | создаёт |
| **П** | правит |
| **У** | удаляет — в этом проекте почти всегда «архивирует» (`deleted_at`). Настоящий `delete` есть ровно в одном месте: отзыв членства |
| **—** | нет доступа: не «кнопки нет», а база не отдаёт строк |

**`registrar` и `finance` — в схеме с 0026–0028.** Гейты RPC — три
предиката (`can_front_desk` = owner/admin/registrar, `can_finance` =
owner/admin/finance, `can_payments` = все четыре; `0026`), политики таблиц —
`tenant_registrar_*` / `tenant_finance_*` по явному списку (`0028`),
`tenant_admin` остаётся у owner/admin. Таблица без вызова `apply_role_rls`
закрыта для новой роли — pgTAP 0028 держит забор по `pg_policies`. DELETE
не выдаётся никому (`0024`).

`registrar` — стойка: ученики, плательщики, расписание, посещения,
абонементы, платежи и рассрочки. Не видит расходов, ставок и зарплат.
`finance` — бухгалтер: платежи, расходы, ставки, зарплаты, периоды, отчёты.
Не создаёт занятий и учеников, не гасит абонементы (возврат — стойка).

Разница между `owner` и `admin` мельче, чем кажется: она есть только в
настройках центра, в управлении персоналом и в `reopen_month`. Во всём
остальном — одно и то же право.

---

## Данные

| Функция | owner | admin | teacher | parent | registrar | finance |
|---|---|---|---|---|---|---|
| **Центр** — карточка и настройки | В П | В | В | В | В | В |
| **Участники и роли** — состав центра | В С П У | В С П У ¹ | В ² | В ² | В ² | В ² |
| **Приглашения** | В С У | В С У ³ | — | — | — | — |
| **Журнал изменений** (`audit_log`) | В | В | — | — | — | — |
| **События** (`events`, outbox) | В | В | — | — | — | — |
| **Специалисты** — карточки | В С П У | В С П У | В ⁴ | — | В | В ⁵ |
| **Ученики** — карточки | В С П У | В С П У | В ⁶ | В ⁷ | В С П У | В ⁵ |
| **Плательщики** — карточки | В С П | В С П | — ⁸ | В ⁹ | В С П | В |
| **Контакты плательщика** — телефон, WhatsApp | В | В | — ⁸ | В ⁹ | В | В |
| **Кабинеты, услуги** | В С П У | В С П У | В | — | В | — |
| **Группы и состав** | В С П У | В С П У | В ¹⁴ | В ¹⁴ | В С П | — |
| **Занятия** — расписание | В С П | В С П | В ¹² | В ¹² | В С П | В ¹⁵ |
| **Состав занятия** (`lesson_participants`) | В | В | В ¹² | В ¹² | В | — |
| **Посещения** (`attendance`) | В С П | В С П | В С ¹⁶ | В ¹² | В С П | В ¹⁵ |
| **Абонементы** — продажа, заморозка, перенос, возврат | В С П | В С П | — ¹⁷ | В ⁷ | В С П ¹⁸ | В |
| **Рассрочки** — план, платёж по строке, отмена | В С П | В С П | — | В ⁷ | В С П | В С П |
| **Платежи** (`payments`) | В С | В С | — | В ⁷ | В С | В С |
| **Расходы и статьи** | В С П | В С П | — | — | — | В С П |
| **Источники оплаты** | В С П | В С П | В | В | В | В С П |
| **Ставки специалистов** (`teacher_rates`) | В С | В С | В ¹⁹ | — | — | В С |
| **Зарплата** — расчёт, корректировки, снимок | В С | В С | В ¹⁹ | — | — | В С |
| **Периоды** — замок месяца | В С | В С | — | — | В | В С ²⁰ |
| **Витрины выручки и кассы** | В | В | — | — | — | В |

¹ Администратор не трогает владельца и другого администратора и может
назначить только роли специалиста, регистратора и бухгалтера (`0028`,
`change_member_role`). Владельца нельзя отключить, если он последний, — это
триггер на `memberships`, а не проверка в функции (`0024`).
² Только собственную строку — `memberships_select_self_or_admin` (`0001:374`).
³ Администратор не может пригласить администратора; владельца нельзя
пригласить никому: роль `owner` вне списка приглашаемых (`0028`).
⁴ Только собственную карточку — `teachers_read_self` (`0004:96`).
⁵ Бухгалтеру ученик и специалист нужны как строка «ФИО + деньги». Клинические
данные этапа 7 (диагностика, цели, ДЗ, заметки занятий) ему закрыты: у их
таблиц не будет политики `tenant_finance_*`. Отступление, зафиксированное в
`reports/stage-5.md`: `students.notes`, `lessons.notes`, `attendance.comment`
бухгалтер сегодня читает прямым запросом — колоночного разделения по роли в
Postgres нет (ADR-005), вынос заметок в отдельные таблицы — этап 7.
⁶ Своих: закреплённых (`primary_teacher_id`) и тех, с кем есть занятия
(`students_teacher_read_own`, `0005_students.sql:145` + расширение `0006`).
Без контактов родителя: колонок телефона и e-mail в витрине нет физически
(`students_teacher_view`, [ADR-005](Decisions/ADR-005-column-privacy.md)).
⁷ Только своих детей — `students_parent_read_own` (`0005:156`) и
`*_parent_read` на абонементах, платежах, рассрочках.
⁸ Специалисту видно одно ФИО плательщика — через `payer_display_name` в
витрине учеников. Доступа к таблице `payers` у него нет.
⁹ Только собственную карточку — `payers_read_self` (`0005:96`).
¹² Только свои занятия (специалисту) и занятия своих детей (родителю) — режет
RLS, а не код: страница для всех ролей одна.
¹⁴ Специалист — свои группы, родитель — группы своих детей (`0006`).
¹⁵ Только чтение: витрины выручки — `security_invoker` с `join lessons`, без
этого бухгалтер видел бы «выручка 0», а не отказ.
¹⁶ Отметить может только на своём занятии (`mark_attendance`,
`teacher_of_lesson`); закрыть занятие — `mark_lesson_status`, только
`done`/`cancelled` и только запланированное.
¹⁷ Специалист не видит `subscriptions` вовсе и получает остаток словом
(`student_subscription_badge`), не числом.
¹⁸ Правка `notes`/`allow_negative` абонемента прямым `update` — решение о
деньгах, остаётся owner/admin (у registrar на `subscriptions` только чтение;
продажа, заморозка, перенос, возврат — через RPC).
¹⁹ Свои строки: `teacher_rates_read_own`, `salary_adjustments_read_own`,
`salary_summary`/`calc_salary` — без цены чужого абонемента.
²⁰ `close_month` — owner/admin/finance; `reopen_month` — только owner.

## Экраны

Страницы, которых роль не должна видеть, делают `redirect('/app')`. Это
косметика: настоящий отказ приходит из базы, а меню просто не показывает
лишнего (`apps/web/app/app/layout.tsx:52`). Экраны этапа 5 (`/app/finance`,
`/app/salary`, `/app/my-salary`, ставки) ещё не построены — строки ниже
описывают, что база уже отдаёт роли.

| Экран | owner | admin | teacher | parent | registrar | finance |
|---|---|---|---|---|---|---|
| `/app` — дашборд | В | В | В ¹⁰ | В ¹¹ | В | В |
| `/app/schedule` — расписание | В | В | В ¹² | В ¹² | В | В ¹⁵ |
| `/app/students` — ученики | В | В | В ⁶ | В ⁷ | В | В ⁵ |
| `/app/students/[id]` — карточка | В | В | В ¹³ | В ¹³ | В | В ⁵ |
| `/app/payers` — плательщики | В | В | — | — | В | В |
| `/app/groups` — группы | В | В | — | — | В | — |
| `/app/debts` — долги | В | В | — | — | В | В |
| `/app/finance` — платежи, расходы, рассрочки, периоды | В | В | — | — | В ²¹ | В |
| `/app/salary` — зарплата | В | В | — | — | — | В |
| `/app/my-salary` | — | — | В ¹⁹ | — | — | — |
| `/app/settings/staff` — сотрудники | В | В ¹ | — | — | — | — |
| `/app/settings/rooms`, `services` | В | В | — | — | В | — |
| `/app/settings/attendance-statuses`, `subscription-types` | В | В | — | — | — | — |
| `/app/settings/teacher-rates` | В | В | — | — | — | В |

¹⁰ Свои занятия на сегодня и счётчик неотмеченных. Ни денег, ни абонементов
(`dashboard-teacher.tsx`).
¹¹ Свои дети: остаток числом, долг, ближайшее занятие (`dashboard-parent.tsx`).
¹³ Без вкладки абонементов (вместо чисел — одно слово из
`student_subscription_badge`), без контактов плательщика и без истории
изменений (`students/[id]/page.tsx:93`).
²¹ Только вкладки «Платежи» и «Рассрочки»: расходов и периодов (кроме
чтения замка) у registrar нет.

---

## Чего в матрице пока нет

Экраны этапа 5 — по мере их появления в `apps/web` (строки выше — по
базе). Этап 6 (уведомления, планировщик `installments_notify`) и этап 7
(клинические данные) — своими строками; для этапа 7 решение по колонкам
`finance` (сноска ⁵) принимается до первой таблицы.
