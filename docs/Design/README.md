# Дизайн

Макеты и токены живут здесь, реализация — в `apps/web`. Правила переноса —
[ADR-007](../Decisions/ADR-007-design-source.md). Коротко: HTML из Stitch не
копируется, переносятся структура и токены на shadcn/ui.

## Состав

| Что | Где | Состояние |
|---|---|---|
| Дизайн-система, токены | `DESIGN.md` | не сгенерирована |
| Скриншоты экранов | `screens/<name>.png` | пусто |
| Ссылки на проекты Stitch | таблица ниже | пусто |

## Экраны

Заполняется по мере генерации. Имя файла совпадает с именем в таблице.

| Экран | Этап | Файл | Ссылка в Stitch |
|---|---|---|---|
| — | — | — | — |

## Что уже построено без макетов

Этапы 0–3 сделаны до появления этого каталога, и перевод на новые токены
будет ретрофитом, а не подгонкой пустого проекта. Масштаб на 10 сентября
2026 года:

- **17 экранов**: `/login`, `/onboarding`, `/select-center`, `/invite/[token]`,
  `/access-revoked`, `/app`, `/app/schedule`, `/app/students`,
  `/app/students/[id]`, `/app/payers`, `/app/payers/[id]`, `/app/groups`,
  `/app/settings/staff`, `/app/settings/rooms`, `/app/settings/services`,
  `/error`, `/`;
- **9 компонентов** в `apps/web/components/ui`: alert, button, card,
  catalog-form, dialog, input, label, select, table;
- **35 CSS-переменных** в `globals.css` — стандартный набор shadcn, без
  единого семантического цвета статуса.

Семантические цвета статусов посещаемости — то, чего сейчас нет вообще, и то,
ради чего дизайн-система нужнее всего: на экране отметки посещаемости статус
читается цветом, а не текстом.

## Как генерировать

Stitch подключается как MCP-сервер, ключ живёт в переменной окружения и
никогда в репозитории. `.mcp.json` здесь не заводится по той же причине.

После подключения сессию нужно перезапустить — новый MCP-сервер подхватывается
при старте.

Лимит около 350 генераций в месяц на аккаунт. Правится конкретный экран, а не
весь проект: перегенерация всего ради одной подписи съедает лимит впустую.
