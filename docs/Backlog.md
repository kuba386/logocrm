# Бэклог

Сюда падают пожелания от живых логопедов — то, что всплывает при показе
staging. Архитектор потом вплетает это в этапы; агент отсюда сам ничего не
берёт в работу.

Формат строки: **что просили** — *кто просил* — почему это важно.

## Не разобрано

<!-- «А можно, чтобы...» — записывайте как услышали, без формулировок в
     терминах базы. Перевод в требования — работа архитектора. -->

**Сохранённые фильтры** — *владелец, 11.09.2026* — расписание и долги каждый
раз фильтруют заново одними и теми же наборами («мои занятия на неделе»,
«долги больше 5 000»). Просят сохранять и давать имя.

**i18n: кыргызский и английский** — *владелец, 11.09.2026* — интерфейс на трёх
языках, переключение в профиле. Здесь именно это — второй и третий язык.
Подготовка к нему в Backlog **не** лежит: начиная с этапа 3 новые строки
интерфейса кладутся в `apps/web/messages/ru.json` и берутся по ключу — это
пункт Definition of Done в [Roadmap/stages.md](Roadmap/stages.md). На момент
записи каталога `messages/` ещё нет: он появляется с первым же экраном,
который пишется после этого правила.

**AI-ассистент администратора** — *владелец, 11.09.2026* — вопрос обычным
языком вместо блуждания по экранам: «какие занятия завтра», «кто должен
денег», «у кого заканчивается абонемент». Ограничение то же, что у поиска:
ассистент отвечает в границах прав спросившего, то есть ходит теми же RPC,
а не читает базу за него.

## Разобрано и вплетено в этапы

| Пожелание | Ушло в этап |
|---|---|
| Отмену абонемента на срок нечем сделать (19.09.2026) | Этап 5, `0054_period_subscription_refund.sql` — формула возврата пропорционально дням, «отменить» отделено от «вернуть деньги» (решения владельца от 23.09.2026) |
| Глобальный поиск (11.09.2026) | `0062_global_search.sql` — одно поле в сайдбаре: имя или телефон → ученик, плательщик, ближайшее занятие. Границы выдачи по ролям = RLS вызывающего (`SECURITY INVOKER`), не второй набор предикатов; бухгалтеру поиск не показывается (у роли нет политик на `students`/`payers`) |
| Приглашение родителя без плательщика (18.09.2026) | Этап 1/8, `0060_parent_invitation_payer.sql` — родитель приглашается только к карточке плательщика (выбор из списка или новая с предупреждением), CHECK на `invitations`, «Привязать плательщика» в «Сотрудниках» для уже существующих «ничьих», три разных пустых состояния в кабинете родителя. Причина оказалась глубже: `create_invitation` никогда не заполняла `payer_id` |
| Отчёты с экспортом CSV/XLSX (11.09.2026) | Этап 5, `0058_report_exports.sql` — `/app/reports`: платежи, зарплата (сводка и детализация), посещаемость, долги; только CSV; деньги — owner/admin/finance, посещаемость — owner/admin; след — событие `report.exported` (решения владельца от 24.09.2026) |
| Кабинет родителя: деньги на разных экранах показаны по-разному (23.09.2026) | Этап 5 — карточка ребёнка отдаёт родителю ту же сводку, что видит администратор (без кнопок продажи/заморозки/возврата); дашборд разворачивает платежи и взносы рассрочки по клику «Показать все» вместо жёсткого обрыва на 10 записях (решения владельца от 24.09.2026) |
